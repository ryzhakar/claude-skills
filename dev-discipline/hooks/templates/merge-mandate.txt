MERGE DECISION MANDATE — Code-quality-reviewer agent (${AGENT_TYPE}) has stopped.

You MUST now read the code-quality-reviewer's A4 report file (extract the path from its return text — see the summary above) and decide the merge action: integrate the worktree branch, continue the implementer via SendMessage with the findings (scoped fixes), fresh-launch a new implementer (fundamental approach failure), or surface to the user with the verdict.

Do NOT proceed to other implementation units or user-facing responses until the merge decision is recorded.
