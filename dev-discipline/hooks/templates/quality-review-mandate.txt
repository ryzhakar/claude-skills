QUALITY REVIEW MANDATE — Spec-reviewer agent (${AGENT_TYPE}) has stopped.

You MUST now dispatch the code-quality-reviewer agent. Provide it with the spec-reviewer's A3 verdict file path (extract from the spec-reviewer's return text — see the summary above) and the implementer's worktree path. The code-quality-reviewer reads A3 itself and short-circuits on a FAIL spec verdict; you do not evaluate the verdict at this boundary.

Do NOT proceed to other implementation units, integration, or user-facing responses until the code-quality-reviewer has returned.
