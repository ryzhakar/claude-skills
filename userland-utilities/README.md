# userland-utilities

Practical utilities for common desktop and system tasks. Includes macOS app bundle repair (Gatekeeper, code signing, quarantine flags).

`macos` `utilities` `gatekeeper` `code-signing` `system` 
## Skills

### [fix-macos-app](skills/fix-macos-app/SKILL.md)

This skill should be used when the user asks to "fix a broken app", "app won't open", "Gatekeeper blocks app", "can't launch app", "app is damaged", "clear quarantine", "re-sign app", "fix code signing", or mentions macOS security preventing an app from launching.

---

