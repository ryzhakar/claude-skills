ARRIVE PROTOCOL — read in this order before dispatching the first agent:

1. ${REF_DIR}/ground-truth.md — task, success criterion, data, protocol, fixed constraints
2. ${REF_DIR}/user_deferred_items.md — the owner's open intentions, in the owner's words
3. ${REF_DIR}/decisions.md — read the tail: tail -n 150 ${REF_DIR}/decisions.md
4. ${REF_DIR}/capabilities.md — what the system is and does
5. ${REF_DIR}/conventions.md — how to work in this project

Answer repo state with git, never from a document: git log --oneline -20 and git status --short.

Write memory on events. A ruling reaches decisions.md in the turn it is spoken; a merge carries its
capabilities.md delta in the same turn; a resolved owner item is deleted. The agentic-delegation
skill governs this order.
