---
name: session-close
description: >
  Retired. Session closure is memento's domain — see memento:span-closure.

  Triggers: "close the session", "do session paperwork", "write the session record",
  "execute the LEAVE protocol", "wrap up the session", "session-close".
---

# Session Close

Retired as a concept. `memento:span-closure` runs the flush, the condensation, the baseline write,
and the waking-cause audit that this skill's LEAVE protocol used to perform by hand. Invoke
`memento:span-closure` directly.
