# Session 2026-09-15

## Binding

Constitution bound at start from source: stop-yapping, first-principles, agentic-delegation. Four
sibling skills referenced by agentic-delegation were not transitively loaded and no claim about them
was bound.

## Phase 1 — The project's skill-writing stance, measured

Two scouts in parallel: a corpus pass measuring all nineteen skills against `ETHOS.md`, and a history
pass mining twelve session records from 2026-04-13 to 2026-08-08.

Findings that overturned the working assumption: `session-close` is the cleanest embodiment of the
ethos, not `agentic-delegation`. XML structure reaches 1 of 19 despite being stated as governing every
skill. Policy-without-rationale reaches 4 of 19, with fifteen files carrying argument. `ETHOS.md` has
three commits total and twice adds a principle in the same commit that first applies it, so the ethos
text trails practice rather than leading it.

The history pass named the recurring enemy: permissive instruction text. Every hedge, condition, or
unstated exclusion reads as license to do less. The corpus pass named the failure skills are built
against: a step reporting success with no verifiable artifact behind it. These are one failure seen at
write time and at run time.

Recorded in `reviews/skill-writing-stance.md`.

## Phase 2 — Foreign ethos comparison

A talk transcript on skill authorship, compared against the recorded stance by the instruction-writer
at opus. Its central structural advice — move branch-specific content behind reference pointers — is
the pattern this project measured failing three times in one session and reversed. The document
undermines it one section earlier by conceding the same unreliability mechanism it then relies on.

Re-cut twice on owner correction. First into a conflict map plus a harvest list, after the initial
framing read as an argument to adopt outside advice. Then into self-contained tables carrying no
identifiers or file references. A deep pass across five discarded seams — decomposed hard conflicts,
mined superficial collisions, re-opened convergences, inert claims naming real problems, and the
document read as an artifact rather than a set of claims — added nineteen items beyond the seven the
whole-claim filter found.

## Phase 3 — The standing record

`skill-governance-and-harvest.md` consolidated: governance as held, the mental model, the conflicts,
and the harvest diff. Anthropic's two skill-authoring skills were then compared against that combined
baseline; ten items survived after cutting everything requiring an evaluation harness and everything
resting on assertion, bringing the harvest to thirty-one. The integration report was folded in and
deleted after section-by-section carry-over verification.

Part 3 was reframed after the owner ruled the current system is not the axis other approaches get
evaluated against. Conflicts are now symmetric: both positions stated as alternatives, what each buys,
what each costs, and the evidence each carries. The reframe surfaced costs on this project's own side
that the verdict framing had hidden, and showed the two reference-pattern rows resting on a single
measurement doing double duty.

## Phase 4 — memento preparation

Memory and record-keeping machinery inventoried marketplace-wide, sliced by file ontology, protocol,
and enforcement. It found a live path contradiction on review artifacts, a living file that had
already broken its own mutability contract, a cross-plugin pointer resolving to nothing, and one
clean case.

The memento draft mapped onto harness mechanics: ninety ideas, roughly sixty grounded, twenty-four
translatable, six ungrounded. Re-run after the not-holy correction, which flipped four verdicts that
had leaned on this project's ontology rather than the harness — including write-your-own-records,
which had borrowed the session-memory rule as though it were a mechanism.

The source system then deconstructed strictly in its own terms, with all harness vocabulary barred:
forty components, nine subsystems, thirteen processes, nine seams, twelve invariants, twelve internal
failure modes, twelve unspecified gaps.

A five-axis harvest established what leaves the orchestration plugin. Only artifact management, record
keeping, and half of session management are textually fused; orientation and discontinuous existence
sit beside that core, joined by shared event types rather than shared mechanism. No machine-readable
schema exists for any Artifact Contract table.

## Phase 5 — The ontology

`reviews/memento-ontology.md`, 431 lines. The naming policy is a recoverability test applied without
exception. Five record tiers built on a persistence-versus-loading split the source's world fused. A
five-class authority model capturing provenance at receipt — the only moment it exists — with the
unenforceable residue named as an invariant rather than papered over. The goal as a parametrizable,
owner-ratified entity whose shape makes the source's catastrophic ending a proposal with no force.

Four of the owner's eight seed correspondences were corrected and stated plainly. Four of five
concerns are fully served; concurrency semantics and routine validation are named unserved among
twelve open questions.

## Failures

Three, in `failures.md`. A false hang from a liveness check run against a symlink. A working agent
killed mid-generation because two zero-delta size samples were read as a hang. `TaskOutput` flooding
orchestrator context permanently after its own deprecation notice was read past.

## In flight

Nothing. Every dispatched agent completed; no crons pending.

## Direction

The ontology defines the system before any skill is drafted. Next is the owner's queued follow-up with
the fable agent, and the twelve open questions the ontology left for downstream.

## Open, awaiting an owner ruling

- Review artifacts: three sources plus `capabilities.md` place them in tracked `history/${DATE}/reviews/`;
  `dev-orchestration`'s Artifact Contract and both reviewer agents write them to gitignored
  `recon/${DATE}/reviews/`.
- `qa-orchestration` names two session-persistence files retired in the 2026-08-08 overhaul.
- `research-tree` gives two unreconciled homes for research output.
- Six paths were staged in the git index by a party other than this session. Left untouched, not
  committed.
