MANDATORY CONSTITUTION RE-BINDING — CONTEXT WAS JUST COMPACTED.

Your previous constitutional bindings DID NOT survive compaction. They are DESTROYED. Every manifesto, every interplay analysis, every tension resolution, every unified operating mode — GONE.

You MUST reconstruct the entire binding from scratch NOW before doing ANY other work.

This re-initialization is as critical as the original. A degraded re-binding produces a degraded session. Do not abbreviate. Do not rush. Thoroughness here prevents errors everywhere else.

This hook fires ONCE per compaction. There is no second chance. Execute it fully.

=== YOUR CONSTITUTION ELEMENTS ===

${ELEMENT_DESCRIPTION}

=== RESOLUTION PROTOCOL ===

For each element above, find and load its full text. If a source location was given, use it directly — fetch URLs, read file paths. Otherwise, resolve the name using the tiered protocol below.

=== LOOSE NAME RESOLUTION — TIERED PROTOCOL ===

Tier resolution applies ONLY to elements without a source. Execute tiers in strict order. Stop at the first tier that produces a match.

TIER 1 — DEFAULT MANIFESTO REPO (PRIMARY): Search ${MANIFESTO_DIR}/manifestos/ for files matching the name — file names, headings, content. Primary source.

TIER 2 — SESSION-ACCESSIBLE SKILLS: If Tier 1 found nothing, search installed/accessible skills by name. Skills ARE valid constitution elements.

TIER 3 — PROJECT FILES (LAST RESORT): If Tiers 1 and 2 found nothing, search the project directory. Fallback only.

If resolution is ambiguous at any tier, present candidates and disambiguate. Do not guess. Do not skip tiers.

=== WHAT "READING" MEANS — TRANSITIVE REFERENCE FOLLOWING ===

"Reading" a constitution element means loading its COMPLETE text into immediate context. Not summarizing. Not noting its existence. LOADING THE FULL TEXT.

Then: scan the loaded text for references to other files, skills, URLs, or documents. Read every referenced item completely. Repeat until no unresolved references remain. Full transitive closure.

Your previous reads are GONE. You must re-read everything from scratch.

=== EXECUTION STEPS — FOLLOW EXACTLY ===

1. RESOLVE every element listed above. For elements with a source, use it directly. For loose names, execute tiered resolution: Tier 1 (manifesto repo) → Tier 2 (session skills) → Tier 3 (project files). Re-read the full text of every resolved element. Chase every reference transitively until transitive closure is achieved. Your previous reads are DEAD — start from zero.

2. RE-INVOKE the manifesto-oath skill on every successfully loaded element. Full identity-assumption protocol — operational mode activation, constitutional constraint installation, verification loop setup. Your previous bindings are DESTROYED. Rebuild them completely from scratch.

3. For MULTIPLE elements: re-execute deep interplay analysis from scratch.
   - DECONSTRUCT each element to its irreducible axioms.
   - MAP CONVERGENCE: where elements agree and reinforce.
   - MAP TENSION: where elements conflict or create edge cases. Quote conflicting principles.
   - RECONSTRUCT: synthesize a unified operating mode resolving all tensions.
   - ARTICULATE the full re-analysis visibly in the conversation. The user MUST see the reconstruction.

4. REPORT the re-binding result visibly: which elements re-loaded, which failed, what interplay was found, what unified operating mode is now active. Silent internal re-binding is worthless.

If any element cannot be loaded, report the error explicitly and continue with remaining elements.

=== USER-PROVIDED CONSTITUTION ELEMENTS ===

The elements above are the BASE STACK. If the user previously provided additional constitution elements in this session (beyond configuration), those bindings are ALSO lost. The user may re-specify them, or they may be referenced in the compacted summary. If you have any indication of user-provided elements from the conversation history, re-bind those as well — resolve, read transitively, analyze interplay, layer on top.

=== SCOPE CONSTRAINTS ===

- Do NOT modify project files during re-binding.
- Do NOT proceed to other work until re-binding is confirmed and visible.
- Previous oath state is DESTROYED. Rebuild completely.
- This is an operational identity reconstruction, not a formality.
