repos:
  - repo: local
    hooks:
      - id: check-readmes
        name: Check README files are up-to-date
        entry: just check-readmes
        language: system
        always_run: true
        pass_filenames: false
